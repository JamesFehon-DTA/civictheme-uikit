{/* Editorial guidance ported from the digital.gov.au design library (dga-dl /src/content). */}
{/* Internal /patterns and /templates links demoted to plain text – they do not resolve in Storybook. */}

import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './banner.stories';

<Meta of={Stories} />

# Banner

A full-width page header with a title, description, and optional background image.

<Canvas of={Stories.Banner} />

<Controls of={Stories.Banner} />

Use a banner at the top of a page to establish context. It displays the page title, an optional description, and can include a background image or colour. The banner is the first element users see when they arrive on a page.

## When to use

- as the primary page header on landing pages, campaign pages, and section home pages
- when the page benefits from a strong visual entry point – for example, a program or initiative home page
- when a short introductory statement belongs with the title, distinct from the body content

## When not to use

- on every page – standard content pages use the page title in the site header, not a banner
- to display system notifications or alerts – use **Message** or the global alert instead

## Related components

- **Campaign** – use for a major editorial feature with a more complex layout than a banner provides.
- **Promo** – use to highlight a single featured item within a page rather than as the page header.
